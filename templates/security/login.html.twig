{# le fichier base.html.twig injecte l'architecture globale #}
{% extends 'base.html.twig' %}

{% block title %}Connexion!{% endblock %}

{% block body %}
{# Formulaire de connexion #}
<form method="post">
{# Gestion des erreurs et des messages à l'utilisateur #}
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {% if app.user %}
        <div class="mb-3">
            Vous êtes connecté en tant que {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Déconnexion</a>
        </div>
    {% endif %}
    {# Les champs email et mot de passe à remplir par l'utilisateur, et un token CSRF unvisible généré automatiquement #}
    <h1 class="h3 mb-3 font-weight-normal">Mot de passe</h1>
    <label for="inputEmail">Email</label>
    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
    <label for="inputPassword">Password</label>
    <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>
    <input type="hidden" name="_csrf_token" data-controller="csrf-protection" value="{{ csrf_token('authenticate') }}">

    {# Une casse à cocher ' Se souvenir de moi ' #}
    <div class="checkbox mb-3">
        <label>
            <input type="checkbox" name="_remember_me"> Se souvenir de moi
        </label>
    </div>

    <button class="btn btn-lg btn-primary" type="submit">
        Se connecter
    </button>
</form>
{% endblock %}
