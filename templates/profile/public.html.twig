{% extends 'base.html.twig' %}

{% block title %} Profil de {{ user.username }} {% endblock %}

{% block body %}
    <div>
        <h2>{{ user.firstname }} {{ user.lastname }}</h2>
        <p>{{ user.getPosts|length }} posts</p>
            {% if user.profileImage %}
                <img src="{{ asset('uploads/profile_images/' ~ user.profileImage) }}" alt="Photo de profil de {{user.username}}">
        {% endif %}
        <button>Suivre</button><br>
        <h2>{{ user.firstname}} {{ user.lastname }}</h2>
        <p>{{ user.username}}</p>
        <p>{{ user.bio }}</p>
        <p>Inscrit le {{ user.createdAt|date('d/m/Y') }}</p>
        <p>
            <strong>{{ user.getFollows|length }}</strong> abonnements                        
            <strong>{{ user.getFollowers|length }}</strong> abonnés
        </p>
        <hr>
        <h3>Publications</h3>
        {% for post in user.posts %}
            <div>
                <p>{{ post.content }}</p>
                <small>{{ post.createdAt|date('d/m/Y H:i') }}</small>
            </div>
        {% else %}
            <p>Aucun post pour le moment</p>
        {% endfor %}
    </div>
{% endblock %}        